# Matplotlib

Documentación técnica de referencia sobre `matplotlib` (y su uso combinado con `seaborn`), generada a partir de los notebooks de práctica del proyecto.

## Contenidos

- [Gráficos de dispersión y línea](./01-Graficos/graficos_de_dispersion_y_linea.md): creación de figuras (`plt.figure()`), gráficos de dispersión (`sns.scatterplot()`) y de línea (`sns.lineplot()`), límites de ejes (`plt.xlim()`, `plt.ylim()`), y guardado/visualización de la figura (`plt.savefig()`, `plt.show()`).

Esta sección se irá ampliando a medida que se incorporen nuevos notebooks con contenido de visualización.
